---
import { Image } from 'astro:assets'
import { getHomepageHero } from '../services/sanity/hero'
import { getImageProps } from '../services/sanity/utils/image'

const homepageHero = await getHomepageHero()
---

<div class="relative overflow-hidden">
  <div class="max-w-screen-xl mx-auto">
    <div
      class="relative z-10 pb-8 bg-white sm:pb-16 md:pb-20 lg:max-w-2xl lg:w-full lg:pb-32"
    >
      <div
        class="pt-10 mx-auto max-w-screen-xl px-4 sm:pt-12 sm:px-6 md:pt-16 lg:pt-20 lg:px-8 xl:pt-28"
      >
        <div class="sm:text-center lg:text-left">
          <h2
            class="text-4xl tracking-tight leading-10 font-extrabold text-gray-900 sm:text-5xl sm:leading-none md:text-6xl"
          >
            {homepageHero.title.slogan}
            <br class="xl:hidden" />
            <span class="text-orange-700">{homepageHero.title.description}</span
            >
          </h2>
          <p
            class="mt-3 text-base text-gray-700 sm:mt-5 sm:text-lg sm:max-w-xl sm:mx-auto md:mt-5 md:text-xl lg:mx-0"
          >
            {homepageHero.summary}
          </p>
          <div class="mt-5 sm:mt-8 sm:flex sm:justify-center lg:justify-start">
            <div class="rounded-md shadow">
              <a
                href={homepageHero.primaryCta.link}
                data-astro-prefetch
                class="font-medium rounded-md w-full flex items-center justify-center px-8 py-3 text-base leading-6 md:py-4 md:text-lg md:px-10 bg-orange-700 hover:bg-orange-600 text-white focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-orange-700"
                >{homepageHero.primaryCta.text}</a
              >
            </div>
            <div class="mt-3 sm:mt-0 sm:ml-3">
              <a
                href={homepageHero.secondaryCta.link}
                data-astro-prefetch
                class="font-medium rounded-md w-full flex items-center justify-center px-8 py-3 text-base leading-6 md:py-4 md:text-lg md:px-10 bg-white text-gray-900 ring-1 ring-inset ring-gray-300 hover:bg-gray-50"
                >{homepageHero.secondaryCta.text}</a
              >
            </div>
          </div>
        </div>
      </div>
      <svg
        class="hidden lg:block absolute right-0 inset-y-0 h-full w-40 text-white transform translate-x-1/2"
        fill="currentColor"
        viewBox="0 0 100 100"
        preserveAspectRatio="none"
      >
        <polygon points="50,0 100,0 50,100 0,100"></polygon>
      </svg>
    </div>
    <div class="lg:absolute lg:inset-y-0 lg:right-0 lg:w-1/2">
      <Image
        alt="Pop'z BBQ logo on wood background"
        class="w-full h-full object-cover"
        decoding="auto"
        loading="eager"
        fetchpriority="high"
        {...getImageProps({
          image: homepageHero.mainImage,
          maxWidth: 1023,
          customWidthSteps: [430, 640, 768, 890, 1023],
          sizes: '(max-width: 1023px) 100vw, 50vw'
        })}
      />
    </div>
  </div>
</div>
